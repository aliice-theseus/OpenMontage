---
name: flux-character-turnaround
description: 使用本地 FLUX.1 Dev 生成用于 3D 建模、角色绑定、服装拆分和 Cosplay 制版的角色横向四视图参考图，包含正面全身、90 度侧面全身、背面全身和胸部以上特写。用户提到三视图、四视图、角色建模参考、人物设定稿、建模三视图、服装拆分参考、正面侧面背面或人体比例参考时使用。
---

# FLUX.1 Dev 角色四视图

生成同一人物、同一服装和统一比例的建模参考图。虽然用户常称“三视图”，本技能的标准交付物是三个全身视角加一个半身特写。

## 前置读取

1. 完整读取 [references/prompt-template.json](references/prompt-template.json)。
2. 在编写或调整提示词前读取 `../flux-best-practices/SKILL.md`。
3. 在 OpenMontage 管道中只通过 `image_selector` 调用 `local_diffusion`，不要直接实例化提供商工具。

## 工作流

1. 从用户请求提取 `dynamic_custom_vars`。未提供的字段使用参考配置中的默认值。
2. 将角色年龄、体型、服装层级、颜色、发型、鞋履和配饰写成明确、无歧义的自然语言。
3. 默认使用完整模板；用户明确要求“快速生成”或“快速模板”时使用 `sub_skill.sub_positive`。
4. 将变量设定追加到正向模板的“人物基础设定”和“服装基础设定”之后。
5. 将负向模板作为质量禁用项读取。FLUX 不支持负面提示词，因此不要传入 `negative_prompt`；改写成正向约束，例如“仅一个人物”“头脚完整可见”“四个视图服装完全相同”“无文字和水印”。
6. 生成前向用户声明工具、提供商、模型和运行模式。基础模型或 LoRA 未缓存时，停止并请求下载确认；未经确认不得设置 `allow_model_download=true`。
7. 生成后按验收清单检查。关键失败最多重试两轮；仍失败则展示当前结果和问题，不宣称其可用于建模。

## 固定调用契约

```python
result = image_selector.execute({
    "prompt": composed_positive_prompt,
    "preferred_provider": "local_diffusion",
    "model": "black-forest-labs/FLUX.1-dev",
    "pipeline_type": "flux",
    "width": width,
    "height": height,
    "num_inference_steps": 28,
    "guidance_scale": 3.5,
    "allow_model_download": False,
    "output_path": output_path,
})
```

尺寸映射：

- `16:9` → `1344 × 768`
- `4:3` → `1024 × 768`
- `1:1` → `1024 × 1024`

用户提供 LoRA 时传递 `lora_model` / `lora_weight_name` / `lora_scale`，并确认 LoRA 基于 FLUX.1 Dev 训练。

## 硬性构图

- 横向四栏：正面全身约 22%，90 度侧面全身约 22%，背面全身约 22%，半身特写约 34%。
- 纯白无缝背景，无场景、道具、装饰、文字或水印。
- 四栏必须是同一人物、同一服装、同一发型、同一配色、同一人体比例和同一棚拍光。
- 三个全身视图必须从头到脚完整可见，标准自然站姿，四肢无遮挡。
- 半身特写必须包含完整头部和双肩，清楚展示五官、发型、领口、肩线、材质与配饰位置。

分栏百分比是生成目标，不是扩散模型的像素级保证。必须检查实际版面；不要仅因提示词包含百分比就判定合格。

## 验收清单

- [ ] 四栏顺序正确，面积接近 22/22/22/34。
- [ ] 只有一个角色身份，没有多余人物或视图重叠。
- [ ] 正面、侧面、背面和特写的脸型、发型与配色一致。
- [ ] 正面严格正对，侧面严格 90 度，背面严格背对。
- [ ] 全身视图头、手、脚完整，无裁切、断肢和明显手足畸形。
- [ ] 服装前后结构、接缝、层次、鞋袜和配件位置可识别且一致。
- [ ] 人体比例自然，肩线和骨盆水平，无夸张动态。
- [ ] 背景纯白，光影均匀，无厚重阴影、文字、水印和噪点。

任何身份不一致、服装漂移、视角错误、头脚裁切或严重肢体畸形均为关键失败。
