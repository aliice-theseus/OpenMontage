# ManimGL 中的 3D

ManimGL 具有强大的 3D 能力，配备灵活的相机系统。与 ManimCE 不同，ManimGL 不需要特殊的 ThreeDScene 类。

## 基本 3D 设置

### 创建 3D 对象

```python
from manimlib import *

class Basic3DScene(Scene):
    def construct(self):
        # 获取相机帧
        frame = self.camera.frame

        # 设置 3D 朝向
        frame.reorient(20, 70)  # theta, phi（度）

        # 创建 3D 对象
        sphere = Sphere(radius=2)
        sphere.set_color(BLUE, opacity=0.7)

        self.add(sphere)
```

## 相机帧控制

### frame.reorient()

`reorient()` 是控制 3D 相机朝向的主要方法。

```python
# frame.reorient(theta, phi, gamma=0, center=ORIGIN, height=8)

# 前视图
frame.reorient(0, 0)

# 等轴测视图
frame.reorient(20, 70)

# 俯视图
frame.reorient(0, 90)

# 侧视图
frame.reorient(90, 90)
```

### 动画化相机移动

```python
class AnimatedCamera(Scene):
    def construct(self):
        frame = self.camera.frame

        # 创建对象
        cube = Cube()
        self.add(cube)

        # 动画化相机旋转
        self.play(frame.animate.reorient(30, 60))
        self.wait()

        # 连续旋转
        frame.add_updater(lambda m, dt: m.increment_theta(0.2 * dt))
        self.wait(10)
```

### 帧方法

```python
# 设置欧拉角
frame.set_euler_angles(theta=30*DEGREES, phi=70*DEGREES)

# 增加角度（用于旋转）
frame.increment_theta(10*DEGREES)
frame.increment_phi(5*DEGREES)
frame.increment_gamma(2*DEGREES)

# 获取当前角度
theta = frame.get_theta()
phi = frame.get_phi()
```

## 3D 几何基本体

### 球体

```python
sphere = Sphere(
    radius=2,
    resolution=(20, 20),  # (u_resolution, v_resolution)
    color=BLUE
)
sphere.set_opacity(0.7)
```

### 立方体

```python
cube = Cube(
    side_length=2,
    color=GREEN,
    fill_opacity=0.8
)
```

### 曲面

```python
# 参数曲面
surface = Surface(
    lambda u, v: np.array([u, v, u**2 + v**2]),
    u_range=(-2, 2),
    v_range=(-2, 2),
    resolution=(20, 20)
)
```

### 环面

```python
torus = Torus(
    r1=2,  # 主半径
    r2=0.5,  # 副半径
    color=YELLOW
)
```

### 圆柱体

```python
cylinder = Cylinder(
    height=3,
    radius=1,
    color=RED
)
```

### 3D 线和形状

```python
# 3D 线
line = Line3D(
    start=[-2, -2, -2],
    end=[2, 2, 2],
    color=WHITE,
    width=0.05
)

# 3D 圆盘
disk = Disk3D(radius=1.5, color=PURPLE)

# 3D 正方形
square = Square3D(side_length=2, color=ORANGE)
```

## 纹理曲面

### 为 3D 对象添加纹理

```python
class TexturedSphere(Scene):
    def construct(self):
        frame = self.camera.frame
        frame.reorient(20, 70)

        # 创建带纹理的球体
        sphere = Sphere(radius=2)

        # 从 URL 或本地文件应用纹理
        textured_sphere = TexturedSurface(
            surface=sphere,
            # 可以使用 URL 或本地路径
            image_file="path/to/texture.jpg"
        )

        self.add(textured_sphere)

        # 旋转相机
        self.play(frame.animate.increment_theta(360*DEGREES), run_time=10)
```

### 地球示例

```python
earth = Sphere(radius=2, resolution=(40, 40))
textured_earth = TexturedSurface(
    earth,
    "https://upload.wikimedia.org/wikipedia/commons/thumb/4/4d/Whole_world_-_land_and_oceans.jpg/1280px-Whole_world_-_land_and_oceans.jpg"
)
```

## 3D 坐标轴

### ThreeDAxes

```python
axes = ThreeDAxes(
    x_range=(-5, 5, 1),
    y_range=(-5, 5, 1),
    z_range=(-5, 5, 1),
    width=10,
    height=10,
    depth=10
)
axes.add_coordinate_labels(font_size=20)
```

### 3D 中的 NumberPlane

```python
# XY 平面
xy_plane = NumberPlane(
    x_range=(-5, 5),
    y_range=(-5, 5)
)

# XZ 平面（水平地面）
xz_plane = NumberPlane(
    x_range=(-5, 5),
    y_range=(-5, 5)
)
xz_plane.rotate(90*DEGREES, axis=RIGHT)
```

## 参数曲面

### 创建自定义曲面

```python
# 抛物面
paraboloid = Surface(
    lambda u, v: np.array([
        u,
        v,
        u**2 + v**2
    ]),
    u_range=(-2, 2),
    v_range=(-2, 2),
    resolution=(30, 30),
    color=BLUE
)

# 波浪曲面
wave = Surface(
    lambda u, v: np.array([
        u,
        v,
        np.sin(u) * np.cos(v)
    ]),
    u_range=(-PI, PI),
    v_range=(-PI, PI),
    resolution=(40, 40)
)
```

### 曲面网格

```python
# 创建曲面
surface = Sphere(radius=2)

# 添加网格覆盖
mesh = SurfaceMesh(surface)
mesh.set_stroke(BLUE, width=1, opacity=0.5)

self.add(surface, mesh)
```

## 在帧中固定对象

### fix_in_frame()

在相机旋转时保持 2D 元素（如标签）固定不动。

```python
class FixedLabels(Scene):
    def construct(self):
        frame = self.camera.frame
        frame.reorient(20, 70)

        # 随相机旋转的 3D 对象
        cube = Cube()
        self.add(cube)

        # 保持固定的 2D 标题
        title = Text("3D Cube", font_size=60)
        title.to_edge(UP)
        title.fix_in_frame()  # 保持在屏幕空间
        self.add(title)

        # 旋转相机
        self.play(frame.animate.reorient(50, 80), run_time=3)
```

## 光照

### 光源

```python
# 访问光源
light = self.camera.light_source

# 移动光源
light.move_to([10, 10, 10])

# 动画化光源移动
self.play(light.animate.move_to([0, 0, 10]), run_time=2)
```

### 光泽和阴影

```python
# 为对象添加光泽
sphere = Sphere(radius=2)
sphere.set_gloss(0.8)  # 0 到 1

# 添加阴影
sphere.set_shadow(0.5)  # 0 到 1
```

## 复杂 3D 场景示例

```python
class Complex3DScene(Scene):
    def construct(self):
        frame = self.camera.frame
        frame.reorient(20, 75)

        # 创建坐标轴
        axes = ThreeDAxes(
            x_range=(-3, 3),
            y_range=(-3, 3),
            z_range=(-3, 3)
        )

        # 创建参数曲面
        surface = Surface(
            lambda u, v: np.array([
                u,
                v,
                np.sin(np.sqrt(u**2 + v**2))
            ]),
            u_range=(-3, 3),
            v_range=(-3, 3),
            resolution=(30, 30),
            color=BLUE
        )
        surface.set_opacity(0.7)

        # 添加网格
        mesh = SurfaceMesh(surface)
        mesh.set_stroke(WHITE, 0.5, opacity=0.3)

        # 添加标题（固定在帧中）
        title = Text("Sinc Function Surface", font_size=48)
        title.to_edge(UP)
        title.fix_in_frame()
        title.set_backstroke(BLACK, 5)

        # 构建场景
        self.add(axes, surface, mesh, title)
        self.wait()

        # 平滑旋转相机
        self.play(
            frame.animate.reorient(45, 70),
            run_time=3
        )
        self.wait()

        # 添加连续旋转
        frame.add_updater(lambda m, dt: m.increment_theta(20 * dt))
        self.wait(10)
```

## 交互式 3D 控制

使用 `manimgl` 运行时，可以与 3D 场景交互：

- 按 `d` + 移动鼠标：旋转相机
- 按 `z` + 滚轮：放大/缩小
- 按 `r`：重置相机到默认位置
- 按 `q`：退出交互模式

## 最佳实践

1. **使用 reorient()**：比手动设置欧拉角更简洁
2. **标签使用 fix_in_frame()**：保持 UI 元素可读
3. **适当的分辨率**：最终渲染用高分辨率，开发时用低分辨率
4. **透明度体现深度**：设置不透明度 < 1 以透视曲面
5. **光照很重要**：调整光源位置以获得更好的可视化效果
6. **连续旋转**：使用 updater 实现平滑相机旋转
7. **帧率考虑**：开发时3D场景可能需要较低帧率

## 常见模式

### 相机绕物体旋转

```python
# 360度旋转
self.play(
    frame.animate.increment_theta(360*DEGREES),
    run_time=10,
    rate_func=linear
)
```

### 创建地平面

```python
floor = NumberPlane(
    x_range=(-10, 10),
    y_range=(-10, 10),
    background_line_style={"stroke_color": GREY, "stroke_width": 1}
)
floor.rotate(90*DEGREES, RIGHT)
floor.shift(2*DOWN)
```

### 多视角

```python
# 从不同角度展示
angles = [(0, 0), (20, 70), (45, 45), (90, 90)]

for theta, phi in angles:
    self.play(frame.animate.reorient(theta, phi))
    self.wait()
```
