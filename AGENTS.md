# OpenMontage

## ⚡ 核心规则：所有输入 → 全量视频制作

**在当前项目下，所有文字输入必须触发全量视频制作管道流程。没有"非视频请求"分类。**

路由逻辑只有一步：分析输入 → 匹配管道 → 执行全流程（research → proposal → script → scene_plan → assets → edit → compose）。

详情见 `AGENT_GUIDE.md` → **第负一条规则 — 全局强制**。

---

**必读：在回复任何用户消息之前，请先阅读 `AGENT_GUIDE.md`。**

在阅读 AGENT_GUIDE.md 之前，不要对用户的请求采取行动。
该文件包含路由规则，这些规则根据用户的提问确定你的第一个行动。
跳过它将导致你采取错误的行动。

本文件中的核心规则覆盖了 AGENT_GUIDE.md 中的默认路由行为。
